#include <stdio.h>

/**
 * main - Entry point
 *
 * while loop operation
 *
 * Return: Always 0 (Success)
 *
 */

int main(void)
{
	int a = 10; /* local variable defination */

	while (a < 20) /* while loop execution */
	{
		printf("value of a : %d\n", a);
		a++;
	}

	return (0);
}
